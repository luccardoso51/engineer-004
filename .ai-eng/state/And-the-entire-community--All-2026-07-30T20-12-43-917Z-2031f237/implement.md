What I built: This ticket already had a substantial prior attempt on the branch (three commits) producing docs/fixture-forensics.md — a standalone, event_id-cited forensic catalog of fixtures/event_sample.jsonl. I reviewed that artifact end-to-end against the actual fixture rather than redoing it. I independently re-verified every claim: the sha256 checksum (1aeb24b415009e89fcf8acb5a178410faf216dc17b16920d9849ecc8bbb24235), the 25-lines / 24-parseable-records / evt-0002-duplicated counts, a real json.loads parse pass confirming line 21 (evt-0020) is the only malformed record, and every per-event verdict and timestamp-diff arithmetic in the doc (the ~7.4s dedup gap, ~47s clock skew on evt-0005, the exact 65-minute matching-millisecond offset on evt-0006, the 2027 future timestamp on evt-0016, the ~50ms bot burst evt-0012..evt-0015, the null tenant_id on evt-0011, the evt-0007 PII, evt-0009 schema drift, and evt-0017 privacy_request). All forensic content, the corrected nine-class taxonomy, the placeholder-divergence rationale, and the six not-at-face-value items checked out as accurate. My code-review found one real defect: the doc cited SCORING.md as "(repo root)", but that file is not committed to this scaffold — the brief references it as ../../SCORING.md (it lives above the challenge folder). I corrected that reference so the citation is accurate and reproducible, matching the honest treatment the doc already gives the uncommitted to-spec.md. No detection code was added — the ticket is documentation-only by design.

Assumptions: I treated the elaborate scoring-doc cross-referencing apparatus (added by the prior attempt's later two commits) as deliberate, in-scope framing rather than scope-creep to strip out, since its citations are accurate once the location is fixed and the instruction was to build on finished work, not redo it. I limited my change to correcting the one factual error.

Test results: Docs-only change; no automated test covers the markdown. The repo's pre-existing scaffold test (tests/test_main.py) currently ERRORS (1 error, 0 pass) — but this predates and is untouched by my change: the naive load_jsonl in the bootstrap scaffold crashes on the malformed evt-0020 record. That is exactly the dead-letter case ticket #2 is explicitly chartered to fix; this ticket's agreed scope forbids adding code, so I did not pull that fix forward. pytest/ruff are not installed in this environment; I ran the suite via `python3 -m unittest`. My forensic verification script (json parse pass) reported 24 parsed OK, 1 failed (line 21) as documented.

Code review: One real finding — SCORING.md was mislocated as "(repo root)" when it is an out-of-repo file the brief links as ../../SCORING.md; fixed. Spec axis: all five acceptance criteria met (checksum, per-event citations, corrected taxonomy with divergence, ≥3 not-at-face-value items, standalone artifact). Standards axis: no code smells apply to a docs-only diff; no repo coding-standards doc exists.

How to test it yourself:
1. From the repo root, verify the recorded checksum matches the fixture: run `shasum -a 256 fixtures/event_sample.jsonl` and confirm it prints 1aeb24b415009e89fcf8acb5a178410faf216dc17b16920d9849ecc8bbb24235, the value in docs/fixture-forensics.md under "Provenance".
2. Open docs/fixture-forensics.md and spot-check any per-event verdict against the raw file — e.g. `sed -n '7p' fixtures/event_sample.jsonl` shows evt-0006 with ts 15:11:03.552Z vs received_at 14:06:03.552Z (the exact 65-minute matching-millisecond skew the doc flags).
3. Confirm the malformed-record claim: run `python3 -c "import json; [json.loads(l) for i,l in enumerate(open('fixtures/event_sample.jsonl'),1) if l.strip()]"` — it raises a JSONDecodeError on the evt-0020 line, matching the doc's "Malformed / corrupt JSON record" class and the ticket-2 dead-letter handoff note.
4. Confirm my fix: `git show HEAD` shows the SCORING.md reference now reads "../../SCORING.md ... not committed to this scaffold" instead of "(repo root)".