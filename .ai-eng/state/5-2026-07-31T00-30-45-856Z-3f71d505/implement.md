What I built: This pass continued on top of the already-committed ticket-#5 work (commit e560273, which expanded the "## AI usage disclosure" section of docs/submission-disclosures.md with the collaboration-model, drafted-vs-directed, and real-experience-grounding subsections covering all four ACs). No new adjustment was requested, so I re-verified the committed disclosure against the repo and found one factual-accuracy defect: the "Directed (not merely approved) #1" example asserted that SCORING.md, scoring_rubric.md, and docs/adr/to-spec.md were all preserved "in the repo," but only scoring_rubric.md actually exists here — SCORING.md is the parent-level (../../SCORING.md) shared challenge-framework doc the brief links to, and docs/adr/to-spec.md is not committed. For a disclosure whose whole value is being a verifiable, non-fabricated audit trail, I reframed that directed moment (commit e7014d7) to keep the genuine "horrified about dropping the docs" quote while citing only verifiable artifacts: scoring_rubric.md as the in-repo result and SCORING.md accurately described as the external framework doc defining the evidence tiers.

Assumptions: SCORING.md living at ../../SCORING.md (outside the repo) is by design — brief.md and scoring_rubric.md both reference it that way, so it is the shared challenge-framework file, not a repo artifact that was lost. docs/adr/to-spec.md is treated as a scoping-time reference that was never committed; the stray reference to it in docs/fixture-forensics.md is outside ticket #5's scope (that file belongs to tickets #2/#3), so I left it untouched.

Test results: No automated tests apply — docs-only Markdown change with no runtime surface; no typecheck/build/suite is relevant. Verification was reading-based: confirmed every file path the disclosure now cites resolves in the repo (10/10 OK), the RabbitMQ grounding quote matches docs/design-pipeline.md line 51 verbatim, and the design doc's "Author context" line states the queue-based + PostHog-SDK-consumer boundary.

Code review: Self-review across both axes — Standards (repo documents none; Fowler smell baseline largely N/A for prose, no duplication/mysterious-naming introduced) and Spec (all four ticket-#5 ACs still covered) — surfaced one real issue: the inaccurate governance-doc citation, now fixed. No remaining findings.

How to test it yourself:
1. Run: git log --oneline 6bdb489...HEAD to see the two commits (e560273 original, e7014d7 accuracy fix).
2. Open docs/submission-disclosures.md and read the "## AI usage disclosure" section with its three subsections: "### Collaboration model", "### What Claude drafted vs. what Lucas directed, approved, or overrode", and "### Where Lucas's real experience grounded or corrected the output".
3. Confirm the four ACs: (a) the "What Claude produced (drafted)" paragraph, (b) the "Directed (not merely approved)" bullets with concrete examples, (c) the "### Collaboration model" subsection documenting the per-decision approve/override rule, (d) the queue-based/RabbitMQ + PostHog-as-SDK-consumer grounding paragraph.
4. Verify the accuracy fix: run grep -n "SCORING.md" docs/submission-disclosures.md and confirm the "Directed #1" bullet no longer claims SCORING.md or docs/adr/to-spec.md sit in the repo, and only cites scoring_rubric.md as the in-repo artifact.
5. Cross-check the grounding citation: run grep -n "This mirrors a RabbitMQ topology" docs/design-pipeline.md docs/submission-disclosures.md and confirm the quoted passage matches the design doc exactly.