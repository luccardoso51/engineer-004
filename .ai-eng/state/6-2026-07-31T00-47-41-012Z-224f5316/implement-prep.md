Build a new standalone file docs/failure-modes.md (title "Failure Modes & What Stays Human") as a distinct packet artifact, separate from docs/submission-disclosures.md, matching how the downstream "Final packet assembly" ticket already treats failure-modes/what-stays-human as its own artifact alongside the evidence log and AI disclosure. The file has two sections. (1) Architecture stress-test: format is table-then-narrative — a scan table (Failure mode | Trigger | What breaks first | Detection | Mitigation) followed by a narrative subsection per scenario, matching the table-then-prose pattern already used in docs/design-pipeline.md. Cover exactly the four scenarios named in the ticket: hot-partition tenants (a single tenant's burst exceeding a Kinesis shard's throughput within its tier, tying to the design doc's tenant-tier-by-Kinesis-stream partitioning and the existing "Burst/spike handling" section), ingestion backpressure (shard/Lambda/DynamoDB-dedupe capacity exhaustion beyond the ~30-shard budget, tying to the design doc's degradation order and IteratorAge alarms), cross-region failover (MVP is single-region us-east-1 per design-pipeline.md with no automated multi-region failover — describe the resulting blast radius/RTO/RPO gap and the human-triggered response, noting EU multi-region is phased for months 4-6), and poison-pill/malformed events — this one must be tied concretely to the actual, already-implemented behavior in src/analytics_pipeline/loader.py: load_jsonl catches json.JSONDecodeError per line, builds a DeadLetter (line_number, raw_text, regex-extracted event_id or "line:N" fallback, error string) without raising, and the loader continues — verified against evt-0020 (file line 21, missing closing brace) in fixtures/event_sample.jsonl per docs/fixture-forensics.md. Draw an explicit distinction between this observed, benchmarked local-script dead-letter behavior (Tier 3/4 evidence) and the still-designed-not-yet-built production SQS DLQ mechanism described in design-pipeline.md, consistent with the packet's evidence-tier discipline. (2) What-stays-human: a single merged table (not split by origin) containing all of: the 4 decisions the ticket names — tenant onboarding exceptions (new tenant traffic profile doesn't fit standard tier assignment), anomaly-detection threshold tuning (grounded in docs/adr/0001-timestamp-anomaly-classification-by-signal-not-id.md's point that fixture-tuned thresholds aren't statistically calibrated for production and need revisiting), compliance sign-off (GDPR/CCPA erasure approval and SOC2-relevant audit review), and incident triage judgment calls (e.g. choosing failover vs throttle vs rollback during a stress-test scenario, trade-off between cost/customer impact/data-loss risk) — merged with the 7 decisions currently listed in docs/submission-disclosures.md's "What stays human" table (GDPR/CCPA erasure approval for evt-0017, quarantine release for evt-0011, tenant tier promotion/demotion, migration promote-per-tenant, bot-flag override for enterprise tenants, shard-split runbook execution, PII quarantine review for evt-0007), deduplicating/rewording overlaps (e.g. folding "GDPR/CCPA erasure approval" together with the new "compliance sign-off" entry) so the new file is the single complete, non-redundant list. As part of this ticket, also trim docs/submission-disclosures.md's existing "What breaks it" and "What stays human" tables down to a one-line pointer to docs/failure-modes.md, so the packet doesn't carry two divergent versions of the same analysis, and update design-pipeline.md's "Operating artifacts" line to list the new file alongside the other excluded-from-page-count artifacts. Additionally, as part of this ticket's implementation commit, also commit the currently-untracked .ai-eng/ folder (harness process artifacts: config.yaml, conventions.json, and per-ticket state/transcripts under .ai-eng/state/ for tickets 2-6) — the user wants this visible to the reviewer as evidence of the harness engineering work done with Nemo. This should be folded into the same commit as the docs/failure-modes.md and related doc-trim changes, not committed separately. No secrets were found in .ai-eng/config.yaml or conventions.json.